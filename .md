```mermaid
flowchart TD
    subgraph prometheus_network
        P0EU1[Prometheus<br/>prometheus0_eu1<br/>:9090]
        TSC_EU1[Thanos Sidecar<br/>thanos_sidecar_eu1<br/>:19090/:19190]
    end

    P0EU1 -- "scraped by" --> TSC_EU1
    TSC_EU1 -- "depends_on" --> P0EU1

    %% Volumes
    V1[(prometheus-storage)]
    P0EU1 --- V1

    %% Config
    CFG1[[prometheus0_eu1.yml]]
    P0EU1 --- CFG1
    TSC_EU1 --- CFG1
```

![Prometheus Thanos Diagram](./my-drow.png)

**Diagram Explanation:**

- **Prometheus (`prometheus0_eu1`)** runs on port 9090, uses a persistent volume, and is configured via `prometheus0_eu1.yml`.
- **Thanos Sidecar (`thanos_sidecar_eu1`)** connects to Prometheus, exposes ports 19090 (HTTP) and 19190 (gRPC), and shares the config file.
- Both services are on the `prometheus_network`.
- The diagram omits commented-out services for clarity.
